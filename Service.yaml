apiVersion: v1
kind: Service
metadata:
  name: nexus-service
  namespace: nexus-repository
spec:
  selector:
    app: nexus-gcs-server
  type: NodePort
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 32000